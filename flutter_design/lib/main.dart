import 'package:flutter/material.dart';
import 'package:flutter_design/design_widget.dart';
import 'flutter_flow_layout.dart';
import 'layout_widget.dart';

void main() {
  // 레이아웃 디자인
  // runApp(const MyApp());
  //  runApp(designApp());
  // runApp(ColumnWidgetExample());
  // runApp(FlexWidgetExample());
  // runApp(ExpandedWidgetExample());
  // runApp(StackWidgetExample());
  runApp(FlutterflowWidgetExample());
}


